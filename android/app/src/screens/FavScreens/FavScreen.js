import React from "react";
import { Text, View } from "react-native";

function FavScreen(){
    return(
        <View style={{flex:1}}>
            <Text style={{textAlignVertical:'center',textAlign:'center'}}>
                This is Favourites recipes Screen.
            </Text>
        </View>
    );
}

export default FavScreen;